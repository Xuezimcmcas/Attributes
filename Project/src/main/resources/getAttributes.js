function getAttributes() {
    var attributes = {};
    attributes["speed"] = new Attribute("speed", 10);
    attributes["strength"] = new Attribute("strength", 5);
    // 在这儿加属性
    return attributes;
}
